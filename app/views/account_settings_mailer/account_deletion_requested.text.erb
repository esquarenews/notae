A Notae account deletion request was submitted for <%= @user.email %>.

Workspace context: <%= @workspace.name %>
Requested at: <%= Time.current.in_time_zone(@user.time_zone).strftime("%d %b %Y %H:%M %Z") %>

This message confirms the request reached Notae. If you did not expect this request, sign in and review your account settings immediately.
