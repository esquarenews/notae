<p>A request to delete the Notae account <strong><%= @user.email %></strong> was started.</p>

<p>This email is a confirmation notice only. The account has not been deleted by this message.</p>

<p>
  Workspace context: <strong><%= @workspace.name %></strong><br>
  Requested at: <strong><%= Time.current.in_time_zone(@user.time_zone).strftime("%d %b %Y %H:%M %Z") %></strong>
</p>

<p>
  If you started this request, keep this email as your record. If you did not expect this request, sign in to Notae and
  review your account settings immediately.
</p>
