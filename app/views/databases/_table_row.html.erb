<% autofocus_title = local_assigns.fetch(:autofocus_title, false) %>
<% highlight_row_id = local_assigns[:highlight_row_id].presence %>
<% linked_page = row.linked_page %>
<% row_can_update = policy(row).update? && !database.locked? %>
<% row_can_destroy = policy(row).destroy? && !database.locked? %>
<% row_style_classes = [] %>
<% row_style_classes << "is-new-row-highlight" if highlight_row_id.to_s == row.id.to_s && row.title.to_s == "Untitled row" %>
<% row_style_classes << "is-row-bold" if row.row_bold? %>
<% row_style_classes << "is-row-italic" if row.row_italic? %>
<% row_color_class = row.row_text_color %>
<% row_style_classes << "is-row-color-#{row_color_class}" unless row_color_class == "default" %>
<% row_background_class = row.row_background_color %>
<% row_style_classes << "is-row-bg-#{row_background_class}" unless row_background_class == "default" %>
<% page_search_url = workspace_document_targets_path(workspace_slug: workspace.slug, kind: "page") %>
<% row_split_params = row_params.merge(
  workspace_slug: workspace.slug,
  id: database.id,
  split_page_id: linked_page&.id,
  split_source: "row",
  split_row_id: row.id
) %>
<tr id="row_<%= row.id %>"
    class="notae-db-grid-data-row <%= conditional_color_class_for_row(row) %> <%= row_style_classes.join(" ") %>"
    data-scroll-preserve-key="row_<%= row.id %>"
    data-db-table-reorder-target="row"
    data-db-table-reorder-row-id="<%= row.id %>"
    data-action="dragover->db-table-reorder#dragover drop->db-table-reorder#drop dragleave->db-table-reorder#dragleave">
  <% name_column_style_classes = name_column_style_classes_for(database) %>
  <td class="<%= name_column_style_classes %>">
    <% row_update_path = database_db_row_path(
      {
        workspace_slug: workspace.slug,
        database_id: database.id,
        id: row.id
      }.merge(row_params)
    ) %>
    <div class="notae-db-row-shell">
      <% if row_can_update %>
        <div class="notae-db-row-hover-controls">
          <% if can_create_rows %>
            <%= button_to database_db_rows_path(workspace_slug: workspace.slug, database_id: database.id),
                          params: row_params.merge(insert_after_id: row.id, db_row: { title: "Untitled row" }),
                          class: "notae-db-row-hover-control",
                          form: { class: "notae-db-row-hover-control-form", data: { preserve_database_scroll: true } },
                          title: "Insert row below",
                          aria: { label: "Insert row below" } do %>
              +
            <% end %>
          <% end %>
          <button type="button"
                  class="notae-db-row-hover-control is-drag-handle"
                  title="Drag to reorder"
                  aria-label="Drag to reorder"
                  draggable="true"
                  data-db-table-reorder-row-id="<%= row.id %>"
                  data-action="dragstart->db-table-reorder#dragstart dragend->db-table-reorder#dragend">
            ⠿
          </button>
          <%= render "databases/row_context_menu",
                     row: row,
                     workspace: workspace,
                     database: database,
                     row_update_path: row_update_path,
                     row_params: row_params,
                     row_color_options: row_color_options,
                     linked_page: linked_page %>
        </div>
      <% end %>

      <div class="notae-db-title-stack">
        <div class="notae-db-title-primary">
          <% if linked_page.present? %>
            <% if row_can_update %>
              <%= form_with model: row,
                            url: row_update_path,
                            method: :patch,
                            class: "notae-db-title-form notae-db-title-form-inline",
                            data: {
                              controller: "auto-submit",
                              auto_submit_focus_on_connect_value: autofocus_title,
                              preserve_database_scroll: true
                            } do |form| %>
                <%= form.text_field :title,
                                    value: row.title,
                                    class: "notae-db-title-input",
                                    placeholder: "Enter name",
                                    data: {
                                      action: "change->auto-submit#submit keydown.enter->auto-submit#submitOnEnter",
                                      auto_submit_create_next_row_on_enter: true
                                    } %>
                <%= hidden_field_tag "db_row[autosave_title]", "1" %>
                <%= form.button "",
                                type: "submit",
                                class: "notae-db-enter-submitter",
                                name: "db_row[create_next_row]",
                                value: "1",
                                style: "position:absolute;left:-9999px;width:1px;height:1px;padding:0;border:0;overflow:hidden;",
                                tabindex: "-1",
                                aria: { hidden: true } %>
              <% end %>
            <% else %>
              <span class="notae-db-title-text"><%= row.title.presence || linked_page.title %></span>
            <% end %>
            <div class="notae-db-row-link-actions">
              <%= link_to database_path(row_split_params),
                          class: "notae-db-row-link-action",
                          title: "Open linked Nota",
                          aria: { label: "Open linked Nota" } do %>
                ↗
              <% end %>
            </div>
          <% elsif row_can_update %>
            <%= form_with model: row,
                          url: row_update_path,
                          method: :patch,
                          class: "notae-db-title-form notae-db-title-form-inline",
                          data: {
                            controller: "auto-submit",
                            auto_submit_focus_on_connect_value: autofocus_title,
                            preserve_database_scroll: true
                          } do |form| %>
              <%= form.text_field :title,
                                  value: row.title,
                                  class: "notae-db-title-input",
                                  placeholder: "Enter name",
                                  data: {
                                    action: "change->auto-submit#submit keydown.enter->auto-submit#submitOnEnter",
                                    auto_submit_create_next_row_on_enter: true
                                  } %>
              <%= hidden_field_tag "db_row[autosave_title]", "1" %>
              <%= form.button "",
                              type: "submit",
                              class: "notae-db-enter-submitter",
                              name: "db_row[create_next_row]",
                              value: "1",
                              style: "position:absolute;left:-9999px;width:1px;height:1px;padding:0;border:0;overflow:hidden;",
                              tabindex: "-1",
                              aria: { hidden: true } %>
            <% end %>
            <%= form_with model: row,
                          url: row_update_path,
                          method: :patch,
                          class: "notae-db-row-link-create-form" do |form| %>
              <%= form.hidden_field :link_action, value: "create_page" %>
              <%= form.button "+",
                              type: "submit",
                              class: "notae-db-row-link-action is-create",
                              title: "Create linked Nota",
                              aria: { label: "Create linked Nota" } %>
            <% end %>
            <details class="notae-db-inline-details notae-db-row-link-chooser">
              <summary class="notae-db-row-link-action is-choose" title="Choose linked Nota" aria-label="Choose linked Nota">🔗</summary>
              <div class="notae-db-inline-panel">
                <%= form_with model: row,
                              url: row_update_path,
                              method: :patch,
                              class: "notae-db-inline-form",
                              data: {
                                controller: "document-picker",
                                document_picker_search_url_value: page_search_url,
                                document_picker_empty_text_value: "No matching notas."
                              } do |form| %>
                  <%= form.hidden_field :linked_page_id, value: linked_page&.id, data: { document_picker_target: "hiddenInput" } %>
                  <%= search_field_tag "db_row_link_page_search_#{row.id}",
                                       "",
                                       placeholder: "Search notas",
                                       autocomplete: "off",
                                       class: "notae-options-input",
                                       data: {
                                         document_picker_target: "searchInput",
                                         action: "focus->document-picker#ensureLoaded input->document-picker#queueSearch search->document-picker#queueSearch"
                                       } %>
                  <p class="notae-document-picker-loading" hidden data-document-picker-target="loading">Searching…</p>
                  <div class="notae-document-picker-results" data-document-picker-target="results"></div>
                  <p class="notae-options-empty" hidden data-document-picker-target="emptyState">No matching notas.</p>
                <% end %>
              </div>
            </details>
          <% else %>
            <span class="notae-db-title-text"><%= row.title %></span>
          <% end %>
        </div>
      </div>
    </div>
  </td>
  <% visible_properties.each do |db_property| %>
    <% db_cell = cells_by_key[[row.id, db_property.id]] %>
    <% property_style_classes = column_style_classes_for(db_property) %>
    <td class="<%= property_style_classes %>">
      <% if db_cell && policy(db_cell).update? && !database.locked? %>
        <%= form_with model: db_cell,
                      url: database_db_cell_path(
                        ({
                          workspace_slug: workspace.slug,
                          database_id: database.id,
                          id: db_cell.id
                        }.merge(row_params)).merge(db_property.progress? ? { progress_inline_update: "1" } : {})
                      ),
                      method: :patch,
                      data: (
                        db_property.progress? ?
                          {
                            turbo_stream: true,
                            preserve_database_scroll: true
                          } :
                          {
                            controller: "auto-submit",
                            turbo_stream: true,
                            preserve_database_scroll: true
                          }
                      ) do |form| %>
          <% field_classes = db_property.select? ? select_input_classes_for(db_property, db_cell.value_text) : "notae-db-cell-input" %>
          <% if db_property.checkbox? %>
            <label class="notae-db-checkbox-wrap">
              <%= form.check_box :value_text,
                                 {
                                   checked: db_cell.value_text == "true",
                                   class: "h-4 w-4 rounded border-stone-300 text-stone-900 focus:ring-stone-400",
                                   data: { action: "change->auto-submit#submit" }
                                 },
                                 "true",
                                 "false" %>
            </label>
          <% elsif db_property.select? %>
            <% blank_label = db_property.name.to_s.strip.casecmp("status").zero? ? "Select status" : "Select option" %>
            <%= form.select :value_text,
                            options_for_select(select_options_for(db_property), db_cell.value_text),
                            { include_blank: blank_label },
                            class: field_classes,
                            data: { action: "change->auto-submit#submit" } %>
          <% elsif db_property.number? %>
            <%= form.number_field :value_text,
                                  value: db_cell.value_text,
                                  id: "db_cell_#{db_cell.id}_value_text",
                                  step: "any",
                                  class: field_classes,
                                  data: { action: "change->auto-submit#submit" } %>
          <% elsif db_property.progress? %>
            <%= render "databases/progress_cell",
                       form: form,
                       db_cell: db_cell,
                       field_id: "db_cell_#{db_cell.id}_value_text" %>
          <% elsif db_property.date? %>
            <%= form.date_field :value_text,
                                value: db_cell.value_text,
                                id: "db_cell_#{db_cell.id}_value_text",
                                class: field_classes,
                                data: {
                                  action: "change->auto-submit#submit",
                                  gantt_grid_row_id: row.id,
                                  gantt_grid_property_id: db_property.id
                                } %>
          <% else %>
            <%= form.text_field :value_text,
                                value: db_cell.value_text,
                                id: "db_cell_#{db_cell.id}_value_text",
                                class: field_classes,
                                data: { action: "change->auto-submit#submit" } %>
          <% end %>
        <% end %>
      <% else %>
        <span class="text-stone-400">-</span>
      <% end %>
    </td>
  <% end %>
  <td></td>
</tr>
